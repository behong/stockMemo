name: cron-ingest

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - name: Call ingest endpoint
        run: |
          curl -fsS "$CRON_URL" > /dev/null
        env:
          CRON_URL: ${{ secrets.CRON_URL }}
